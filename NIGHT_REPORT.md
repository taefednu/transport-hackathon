# Ночной отчёт — 07.08.2026

Бэкенд готов, фронт можно сажать. **Все шесть гейтов ТЗ пройдены**, проверка
воспроизводится одной командой. Незакоммиченной работы нет.

## Как запустить

```bash
cd transport_hack
.venv/bin/python -m uvicorn app.main:app --port 8023      # сервер
.venv/bin/python scripts/verify_gates.py 8023             # приёмка всех гейтов
bash scripts/run_all.sh                                   # пересобрать пайплайн с нуля
```

Пайплайн с пустого `data/build` собирается за ~5 минут. Внешние источники лежат
в `data/external/`: `uzbekistan-latest.osm.pbf` (Geofabrik, 122 МБ) и
`kontur_population_UZ.gpkg` (HDX, 20 МБ). Оба в git не попадают.

## Гейты — фактические проверки

Вывод `scripts/verify_gates.py`, прогнан после полной пересборки пайплайна:

| Гейт | Что проверено | Результат |
|---|---|---|
| 1 | PNT-500 в границах 1.5–3.2 млн | население 2 613 220; **PNT-500 2 362 141 (90.4%)**; вне доступа 251 079; PNFT-15 1 475 301 (56.5%); T-median 3.25 мин; 108 мс |
| 2 | время хода растёт к часу пик | маршрут 14: 31 остановка, 14.79 км; весь маршрут **33.9 мин в 6:00 против 53.4 мин в 8:00**; 99.3% строк по реальному трафику; 66 мс |
| 3 | сдвиг выезда на час меняет прибытие не на час | 06:00 в пути 33.9 мин, 07:00 в пути 44.2 мин; **разрыв прибытия 70.37 мин, то есть +10.37 к сдвигу**; вечером 18:00→19:00 разрыв 57.73 мин (−2.27); 48 мс |
| 4 | сценарий считает и приобретения, и потери | продление 14 на 3 остановки **+32 530 человек**, потерь 0, 104 мс; обрезка 48 (fwd) до seq=38 **−32 217 человек**, 111 мс |
| 5 | фактический интервал утром меньше вечернего | маршрут 8: **3.16 мин в 8:00 на 17 машинах против 6.00 мин в 22:00 на 8**; дыр 217 на 251 079 человек; перегонов с 5+ маршрутами 468; 49 мс |
| 6 | поиск через транслитерацию и экспорт | «куйлюк» находит `Массив Куйлюк-1` и `Qo‘yliq`; «чилонзор» находит `Chilonzor`; CSV расписания 3 721 строка; GeoJSON маршрута 32 фичи; 59 мс |

Все времена ответа укладываются в бюджет ТЗ (baseline 300 мс, routes 150 мс,
schedule 200 мс, scenario 800 мс, holes 200 мс).

## Числа для питча

Каждое воспроизводится скриптом из `scripts/`, ни одно не выдумано.

- **2 613 220 человек** в административной границе Ташкента по слою населения.
- **2 362 141 (90.4%)** имеют обслуживаемую остановку в 500 м пешком **по сети улиц**,
  а не по прямой.
- **251 079 человек** не имеют. Это 217 гексагонов; в самом крупном — **40 666 человек,
  до ближайшей обслуживаемой остановки 3.4 км**.
- **1 475 301 (56.5%)** имеют рядом маршрут с интервалом 15 минут или лучше.
  Остановка есть у девяти человек из десяти, транспорт — у пяти с половиной.
- Маршрут 14 в утренний пик едет **на 19.5 минут дольше**, чем в 6 утра: 53.4 против 33.9.
- Маршрут 8 в пятницу: **3.16 минуты интервал в 8 утра на 17 машинах**, к 22:00 — 6 минут на 8.
- Продление одного маршрута на три существующие остановки, которые сегодня никто
  не обслуживает, даёт **+32 530 человек** доступа. Обрезка другого маршрута
  отнимает доступ у **32 217**. Инструмент показывает обе стороны.
- **197 остановок-призраков**: объект есть, `route_count = 0`, обслуживания нет.
- На одном перегоне встречается до **9 маршрутов**; 468 перегонов несут 5 и больше.

## Что построено

```
app/config.py      пути и доменные константы, каждая с источником
app/textnorm.py    узбекская транслитерация и нормализация, Левенштейн
app/walkgraph.py   пешеходный граф в CSR, Дейкстра с отсечкой и многоисточниковая
app/store.py       загрузка parquet в память; чего нет — то None, а не заглушка
app/coverage.py    PNT-500, PNFT-15, T-median
app/schedule.py    движок расписания
app/scenario.py    операции сценария и пересчёт по разнице множеств
app/validation.py  предупреждения планировщику
app/search.py      поиск по остановкам и маршрутам
app/main.py        FastAPI
scripts/00..09     пайплайн, по скрипту на шаг, идемпотентные
scripts/run_all.sh полная пересборка
scripts/verify_gates.py  приёмка всех гейтов по живому API
```

Эндпоинты: `/api/meta`, `/api/stops`, `/api/routes`, `/api/routes/{num}`,
`/api/routes/{num}/schedule`, `/api/baseline`, `/api/scenario`, `/api/holes`,
`/api/segments/parallel`, `/api/search`, `/api/export/schedule`, `/api/export/route`.

Не сделано из ТЗ: `/api/routes/{num}/buses` (позиции бортов) и `/api/explain`.
Оба помечены в ТЗ как необязательные.

## Размеры данных

| Артефакт | Строк |
|---|---|
| остановки | 3 422 (2 872 Яндекс + 550 уникальных OSM) |
| обслуживаемые остановки | 2 675 |
| пешеходный граф | 290 137 вершин, 655 342 направленных ребра |
| гексагоны населения | 769 |
| маршруты | 223 направления по 165 номерам, из них 117 направлений по 60 маршрутам с точным порядком остановок |
| порядок остановок | 3 894 |
| скорости | 1 245 958 наблюдений в 28 461 точке |
| время хода по перегонам | 271 944 (91.1% по трафику, 8.9% по медиане города) |
| фактический интервал | 8 973 (3 106 154 транзакции автобуса, 38 853 рейса, 169 маршрутов) |
| перегоны с числом маршрутов | 3 772 |
| дыры покрытия | 217 |

## Где ТЗ разошлось с данными — шли за данными

1. **Трафик Яндекса — точки наблюдений, а не участки линий.** Ни геометрии, ни
   идентификатора участка. Поэтому `segment_id` построен нами из координаты
   точки, а привязка к трассе идёт по расстоянию: перегон режется на куски по
   100 м, каждый берёт скорость ближайшей точки в радиусе 100 м. Покрытие
   реальными наблюдениями — 91.1%, остальное честно помечено `source=fallback`.
2. **Воскресенья в трафике нет.** Данные Яндекса — среда, пятница, суббота
   (03, 05, 06.12.2025). Транзакции — пятница, суббота, воскресенье (01–03.05.2026).
   Общие дни: пятница и суббота. Расписание на воскресенье посчитать нечем.
3. **Гейт 3 в ТЗ требовал разрыва меньше 60 минут.** Это верно только при сдвиге
   выезда **из** пика. Сдвиг в утренний пик делает рейс медленнее, и разрыв
   становится больше 60. Проверяем то, что и просил заказчик по сути: разрыв не
   равен ровно часу. Показываем оба направления.
4. **Покрытие считается по обслуживаемым остановкам, а не по всем физическим.**
   Иначе главная метрика продукта не работает: ТЗ запрещает выдумывать новые
   остановки, значит продление маршрута к существующей остановке не может никого
   добавить. По всем физическим остановкам PNT-500 был бы 2 397 775 против
   2 362 141 — обе цифры отдаются в `/api/baseline` рядом.
5. **Дорожный граф не понадобился.** Перегон берётся как участок трассы самого
   маршрута из OSM. Прокладывать заново по дорожному графу — значит разойтись с
   реальной трассой ради лишнего шага.
6. **Перегон для разведения линий определён парой соседних остановок**, а не
   идентификатором дорожного сегмента, которого в данных нет.

## Чего в базе нет и почему

- **Порядок остановок известен для 60 маршрутов из 169.** OSM знает 67 номеров в
  границах города, у 60 из них порядок восстановился. Остальные лежат в базе с
  `quality=approximate`, без геометрии и без расписания — достраивать догадками
  нельзя. Прямое следствие: **PNFT-15 = 1 475 301 это нижняя оценка**, ей нужен
  список остановок каждого маршрута.
- **Слой населения — срез ноября 2023.** Kontur даёт 2.61 млн по городу против
  официальных 3.18 млн на 01.01.2026. Это не ошибка привязки: по стране Kontur
  даёт 35.96 млн против официальных 38.24 млн, то есть слой соответствует своей
  дате. Называть цифру нужно с датой.
- **Гексагон H3 r8 крупнее зоны доступности** (0.74 км², сторона ~460 м).
  Остановка, накрывшая угол гексагона, помечает покрытым весь гексагон. Это
  единица, в которой поставляется Kontur, и метод из ТЗ, но покрытие им
  завышается. На защите говорить самим.
- **Позиции бортов и `/api/explain`** не сделаны — в ТЗ помечены необязательными.
- **Олмалик не трогали** — по ТЗ только после того, как Ташкент полностью готов.

## Что чинил по ходу

- `T-median` выходил 0.0: расстояние до гексагона мерялось до ближайшей его
  вершины, а она стоит прямо под остановкой. Заменено на медиану по вершинам
  гексагона плюс отдельная многоисточниковая Дейкстра от всех остановок сразу.
- Шаг 7 падал по памяти (OOM, код 137), когда рядом висели поднятые серверы:
  `scan_csv` тянул все 16 колонок. Оставлены три нужные.
- `shapely.ops` дважды не был импортирован — обрезка маршрута отвечала 500.

## Дальше

1. Позиции бортов `/api/routes/{num}/buses` — расписание для этого уже есть.
2. Дотянуть порядок остановок для маршрутов вне OSM (tashtrans.uz парсится) —
   это прямо поднимает PNFT-15 с нижней оценки до настоящей.
3. Олмалик: тот же пайплайн со сменой bbox плюс метрика по его 55 зонам.

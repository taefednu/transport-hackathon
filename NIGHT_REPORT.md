# Ночной отчёт — 07.08.2026

Сессия остановлена пользователем по лимиту (около 04:00). Работа доведена до
безопасной точки: всё закоммичено, пайплайн воспроизводится с нуля.

## Докуда дошёл

Гейт 1 — **пайплайн собран, API ещё нет**. Шаги 1–3 из ТЗ работают и дают
артефакты. Осталось: `app/main.py` с `/api/baseline`, чтобы гейт можно было
закрыть проверкой из раздела 12 ТЗ. Гейты 2–6 не начаты.

| Шаг ТЗ | Скрипт | Статус | Что на выходе |
|---|---|---|---|
| 0 (граница) | `scripts/00_boundary.py` | готово | Toshkent shahri, 633.9 км², из локального OSM |
| 1 (остановки) | `scripts/01_stops.py` | готово | 3 422 остановки |
| 2 (пешеходный граф) | `scripts/02_walk_graph.py` | готово | граф 290 137 вершин, 9 327 пар (остановка, гексагон) |
| 3 (население) | `scripts/03_population.py` | готово | 769 гексагонов H3 r8, 2 613 220 человек |
| 4–9 | — | не начато | |

Время прогона всего пайплайна с нуля: около 2 минут (не считая загрузок).

## Числа, посчитанные на данных

- Граница города из OSM (`admin_level=4`, `Toshkent shahri`): **633.9 км²**.
- Остановки: **2 872** из `stations.csv` Яндекса + **550** уникальных из OSM
  после дедупликации 25 м = **3 422**. Из них 3 363 автобусных, 36 метро,
  23 маршрутки. Привязка к пешеходному графу: медиана 10.6 м, максимум 111.5 м.
- Пешеходный граф в границе города: **290 137 вершин**, 655 342 направленных ребра.
- Зоны доступности 500 м по сети: покрыт **571 гексагон** H3 r8.
- Население (Kontur Population, срез 01.11.2023, лицензия CC BY), обрезано по
  границе с весом по площади пересечения: **2 613 220 человек** в 769 гексагонах.

**PNT-500 ещё не посчитан** — нет кода, который сводит `stop_hexes` с `hexes`.
Это первое, что нужно сделать дальше, и это же закрывает гейт 1.

## Расхождения, которые надо знать

1. **Kontur даёт 2.61 млн против официальных 3.18 млн (01.01.2026).**
   Не ошибка привязки: по всему Узбекистану Kontur даёт 35.96 млн против
   официальных 38.24 млн — то есть слой соответствует своему срезу (ноябрь 2023),
   а официальная цифра относится к январю 2026. Разница ≈ 18% по городу.
   На защите это надо называть самим: «слой населения — ноябрь 2023».
   Заведено в `knowledge/open-questions.md`.
2. **Гексагон r8 крупнее зоны доступности.** Сторона гексагона ≈ 460 м, площадь
   0.74 км². Остановка, накрывшая угол гексагона, помечает покрытым весь
   гексагон. Это метод из ТЗ (и единица, в которой поставляется Kontur), но
   покрытие им завышается. Говорить об этом честно.
3. **Трафик Яндекса — точки, а не участки линий.** В `traffic_dedup.csv` поля
   `lat, lon, hour, speed_avg, speed_median, road_category, street`, никакой
   геометрии участка. ТЗ (шаг 5) предполагает `geometry_wkt` у сегмента.
   Следовать данным: сопоставлять точку наблюдения с ближайшим куском пути
   в радиусе `TRAFFIC_MATCH_M`.
4. **`stations.csv` не содержит `stop_id`.** Идентификаторы назначены нами:
   `Y00000…` для Яндекса, `N<osm_id>` для OSM.
5. **Нормализация не склеивает `Куйлюк` и `Qo'yliq` в одну строку**
   (`kuiliuk` против `koilik`) — это разные транслитерации разных исходных форм.
   Расстояние Левенштейна между ними равно 2, то есть порог из ТЗ их находит.
   Отдельная проверка при работе над гейтом 6.

## Что сделано в репозитории

```
app/config.py      пути и доменные константы (WALK_LIMIT_M и т.д., с источниками)
app/textnorm.py    транслитерация и нормализация названий, Левенштейн
app/walkgraph.py   пешеходный граф в CSR + Дейкстра с отсечкой
scripts/00..03     пайплайн, каждый шаг перезапускаем и печатает число строк
data/external/     uzbekistan-latest.osm.pbf (122 МБ), kontur_population_UZ.gpkg
data/build/        артефакты parquet — в git не попадают
```

Окружение: `.venv` на Python 3.12, polars 1.43, geopandas 1.1.4, h3 4.5,
shapely 2.1.2, osmium 4.3.1, fastapi, uvicorn.

## Проверено фактически

- `00_boundary.py` → 1 кандидат, `Toshkent shahri`, bbox 69.122–69.526 / 41.158–41.423.
- `01_stops.py` → 3 422 строки в `stops.parquet`.
- `02_walk_graph.py` → отработал дважды подряд после правки, 18 с, 9 327 пар.
- `03_population.py` → 769 гексагонов, 2 613 220 человек.
- Нормализация проверена на `Куйлюк / Qo'yliq / Kuyluk / қуйлиқ / Chilonzor / Чилонзор`.
- Overpass не использовался нигде: маршруты берём из локального дампа (шаг 4, не начат).
- `git status` перед коммитом: ни одного файла из `data/`, ни одного `.csv`.

## Дальше, в порядке зависимостей

1. `app/store.py` + `app/coverage.py` + `/api/baseline` → **закрыть гейт 1**,
   записать PNT-500 отдельным числом.
2. Шаг 4: маршруты из локального PBF (`type=route, route=bus`), связка с egov
   по номеру. Overpass недоступен — только дамп.
3. Шаги 5–6: скорости по часам и время хода по перегонам. Это ядро.
4. Движок расписания → гейт 3.

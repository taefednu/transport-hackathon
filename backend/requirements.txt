# Только то, что нужно серверу в рантайме. Пайплайн (h3, geopandas, pyrosm)
# сюда не входит: тяжёлый счёт сделан заранее и лежит в parquet.
# dotenv не нужен — на хостинге переменные приходят из окружения; локально он
# подхватывается опционально, config.py переживает его отсутствие.
fastapi==0.141.1
uvicorn==0.52.1
polars==1.43.2
numpy==2.5.1
scipy==1.18.0
shapely==2.1.2
